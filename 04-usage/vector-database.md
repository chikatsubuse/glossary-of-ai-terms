# ベクトルDB（Vector Database）

- ベクトルDB＝エンベディングを大量に保存し、「意味が近い順」に高速検索できるデータベース
- 質問をベクトル化し、保存済み文書ベクトルとの近さで上位を返す（類似度検索）
  - 数百万件からミリ秒で探すための近似アルゴリズムが中核技術
- RAGの標準部品として普及。専用製品（Pinecone等）に加え、PostgreSQL等の既存DB拡張でも実現できる
- ベクトル検索単体は取りこぼしもあり、キーワード検索と組み合わせるハイブリッド検索が精度の定石
