# Structured Output（構造化出力）

- Structured Output＝出力を自由文ではなく、指定した形式（JSON等）に厳密に従わせる機能
- 出力の型をあらかじめ定義し、モデルがそれ以外を返せないよう生成段階で強制する
  - 「だいたいJSONで返して」というプロンプト頼みと違い、形式崩れが原理的に起きない
- AIの出力を後続システムへ流し込む連携の要。抽出・分類・データ化業務を安定運用できるようになった
- 形式の強制で精度が微妙に変わる場合があり、思考は自由文で行わせ最終出力だけ構造化する使い分けもある
