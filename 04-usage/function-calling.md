# Function Calling

- Function Calling＝モデルが「この関数をこの引数で実行して」と構造化された形で返す仕組み
- モデル自身はコードを実行せず、実行はアプリ側が担い、結果をモデルに返して続きを生成する
  - 「天気を調べる」「DBを検索する」「メールを送る」など、外部機能の呼び出し判断を任せられる
- 2023年にOpenAIがAPIへ導入した呼称で、LLMを「話すだけ」から「操作する」存在に変えた転換点
- どの関数をいつ呼ぶかの判断精度がモデルの重要な性能指標となり、エージェントの基礎能力とされる
