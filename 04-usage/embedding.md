# エンベディング（Embedding）

- エンベディング＝テキストなどの意味を、数百〜数千個の数値の並び（ベクトル）に変換したもの
- 意味が近い内容ほど、近い数値になるように作られる
  - 「請求書の締め日」と「支払期限はいつ」のような、言葉が違っても意味が近いペアを数学的に見つけられる
- キーワード一致に頼らない「意味での検索」を可能にし、検索・分類・レコメンド・重複検出の基盤になる
- 生成には専用のエンベディングモデルを使う。LLM本体とは別に選定・運用する部品であることは見落とされやすい
